# 配置模块

## 说明
- 管理游戏相关配置

## 类名
- UMConfig

## 功能

### 获取配置表
```
T GetTable<T>()
```
- `T`配置表类型

## 初始化参数

### 类名
- UMConfigInitArgs

### 属性

#### LoadConfigHandler
- 用于处理加载配置的具体操作
- 模块已经定义的处理器
    - `UMResLoadConfigHandler`从Unity Resources文件夹加载配置
- 支持自定义扩展
    - 扩展接口`IUMLoadConfigHandler`